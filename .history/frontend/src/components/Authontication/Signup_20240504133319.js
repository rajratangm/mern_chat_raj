import { FormControl, FormLabel, Input, VStack } from '@chakra-ui/react'
import {Input} from '@chakra-ui'
import React from 'react'

export const Signup= () => {
  return (
    <VStack spacing='5px'>
        <FormControl>
            <FormLabel>

            </FormLabel>
            <Input></Input>
        </FormControl>

    </VStack>
  )
}
