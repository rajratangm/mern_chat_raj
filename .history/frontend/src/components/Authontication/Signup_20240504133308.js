import { FormControl, FormLabel, Input, VStack } from '@chakra-ui/react'
import {Input} from '@c'
import React from 'react'

export const Signup= () => {
  return (
    <VStack spacing='5px'>
        <FormControl>
            <FormLabel>

            </FormLabel>
            <Input></Input>
        </FormControl>

    </VStack>
  )
}
